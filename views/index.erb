
<% unless @memos.empty? %>
<ul>
  <% @memos.each do |memo| %>
  <li><a href='<%= "/memos/#{memo.memo_id}" %>'><%= "#{trancate(first_line(memo.content))}" %></a></li>
  <% end %>
</ul>
<% else %>
  <div>
    Memo Not Found.
  </div>
<% end %>
